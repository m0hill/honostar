// Minimal Honostar runtime placeholder.
//
// Honostar's server renderer already injects the critical fetch/tab-id bootstrap inline.
// This module is intentionally tiny; add app-specific client enhancements here if desired.
export {}
